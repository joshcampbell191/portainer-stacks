version: '2'
services:
  airconnect:
    image: 1activegeek/airconnect
    restart: unless-stopped
    environment:
      - AIRCAST_VAR=-x /config/config.xml
      - AIRUPNP_VAR=kill
    network_mode: host
    hostname: airconnect
    volumes:
      - config:/config
volumes:
  config:
