version: '2'
services:
  airconnect:
    image: 1activegeek/airconnect:1.9.3
    restart: unless-stopped
    environment:
      - AIRCAST_VAR=-x /config/config.xml
      - AIRUPNP_VAR=kill
    network_mode: host
    volumes:
      - /volume1/docker/airconnect/config:/config:rw
